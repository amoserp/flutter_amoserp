import 'package:flutter/material.dart';

class AmosERPPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('AmosERP'),),
    );
  }
}